import React from 'react';
import { connect } from 'react-redux';
import numeral from 'numeral';

const ExpenseSummary = ({ expenseCoun, expenseTotal }) => {
   return (
   	<div>
   	  <h1>Viewing {expenseCount} totaling
   	</div>
   	)
};
